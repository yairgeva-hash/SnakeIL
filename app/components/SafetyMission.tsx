import type { SafetyQuestion } from "../types/game";
import { answerClass } from "../lib/game";
import { ChallengeHeader, Feedback } from "./GameUI";
export function SafetyMission({ question, index, total, selected, feedbackLine, onClose, onChoose, onNext }: { question: SafetyQuestion; index: number; total: number; selected: number | null; feedbackLine: string; onClose: () => void; onChoose: (index: number) => void; onNext: () => void }) {
return <section className="challenge notebook-page"><ChallengeHeader onClose={onClose} progress={((index + 1) / total) * 100} icon="🛡️" /><div className="challenge-card"><span className="question-label">משימת בטיחות {index + 1}/{total}</span><div className="scene-panel"><span>דיווח שטח</span><strong>🐍</strong><small>עוצרים ומתבוננים מרחוק</small></div><h1>{question.scene}</h1><div className="choice-list">{question.choices.map((choice, choiceIndex) => <button key={choice} className={answerClass(selected, choiceIndex, question.correct)} onClick={() => onChoose(choiceIndex)}>{choice}</button>)}</div>{selected !== null && <Feedback isCorrect={selected === question.correct} line={feedbackLine} explanation={question.explanation} onNext={onNext} isLast={index === total - 1} />}</div></section>;
}
