import type { Mission } from "../types/game";
export function JourneyScreen({ missions, completed, onOpen, onHome, onReset }: { missions: Mission[]; completed: string[]; onOpen: (mission: Mission) => void; onHome: () => void; onReset: () => void }) {
return <section className="content notebook-page journey"><div className="section-heading"><div><span className="eyebrow">משלחת הכרמל · פרק 1</span><h1>הופכים לבלשי נחשים</h1><p>כל תחנה מלמדת מיומנות אחת וממלאת עוד עמוד ביומן השדה.</p></div><button className="ghost" onClick={onHome}>חזרה למחנה</button></div><div className="lesson-path">{missions.map((mission, index) => { const done = completed.includes(mission.id); return <div className={`lesson-row ${index % 2 ? "offset" : ""}`} key={mission.id}><button className={`lesson-node ${done ? "done" : ""} ${!mission.available ? "locked" : ""}`} onClick={() => onOpen(mission)} disabled={!mission.available}><span className="lesson-icon">{done ? "✓" : mission.icon}</span><span><strong>{mission.title}</strong><small>{mission.subtitle}</small></span><b>{mission.available ? `+${mission.xp} XP` : "🔒"}</b></button>{index < missions.length - 1 && <div className="path-line" />}</div>; })}</div><button className="text-button" onClick={onReset}>איפוס התקדמות במכשיר</button></section>;
}
